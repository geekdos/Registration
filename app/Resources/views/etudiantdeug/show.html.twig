{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Vous ete bien inscrit dans la Filiére <code>{{ etudiantDeug.filiere.nom }}</code></h1>
    <a href="{{ path('etudiantDeug_edit', { 'id': etudiantDeug.id }) }}" class="btn btn-info pull-left">Modifier | تغيير</a>
    {{ form_start(delete_form) }}
    <input type="submit" class="btn btn-danger pull-right" value="Effacer | حذف">
    {{ form_end(delete_form) }}
    <div class="panel panel-purple">
        <div class="panel-heading clearfix">
            <h4 class="panel-title">Vérifier Bien Vos informations avant de générer le PDF | عليك أن التحقق من المعلومات الخاصة بك قبل إنشاء بدف</h4>
        </div>
        <div class="panel-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>CNE</th>
                    <td>{{ etudiantDeug.cne }}</td>
                </tr>
                <tr>
                    <th>Nationalité</th>
                    <td>{{ etudiantDeug.nationalite }}</td>
                </tr>
                <tr>
                    <th>Nom en Français</th>
                    <td>{{ etudiantDeug.nomfr }}</td>
                </tr>
                <tr>
                    <th>Prenom en Français</th>
                    <td>{{ etudiantDeug.prenomfr }}</td>
                </tr>
                <tr>
                    <th>Nom en Arabe</th>
                    <td>{{ etudiantDeug.nomAr }}</td>
                </tr>
                <tr>
                    <th>Prenom en Arabe</th>
                    <td>{{ etudiantDeug.prenomAr }}</td>
                </tr>
                <tr>
                    <th>Genre</th>
                    <td>{{ etudiantDeug.sexe }}</td>
                </tr>
                <tr>
                    <th>L'état civil</th>
                    <td>{{ etudiantDeug.etatCivil }}</td>
                </tr>
                <tr>
                    <th>Handicap</th>
                    <td>{% if etudiantDeug.handicap %}Oui{% else %}Non{% endif %}</td>
                </tr>
                <tr>
                    <th>Bourse</th>
                    <td>{% if etudiantDeug.bourse %}Oui{% else %}Non{% endif %}</td>
                </tr>
                <tr>
                    <th>Lieu de naissance en Français</th>
                    <td>{{ etudiantDeug.lieuNaissFr }}</td>
                </tr>
                <tr>
                    <th>Lieu de naissance en Arabe</th>
                    <td>{{ etudiantDeug.lieuNaissAr }}</td>
                </tr>
                <tr>
                    <th>Date de naissance</th>
                    <td>{% if etudiantDeug.dateNaiss %}{{ etudiantDeug.dateNaiss|date('Y-m-d') }}{% endif %}</td>
                </tr>
                <tr>
                    <th>CIN</th>
                    <td>{{ etudiantDeug.cin }}</td>
                </tr>
                <tr>
                    <th>GSM</th>
                    <td>{{ etudiantDeug.gsm }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ etudiantDeug.email }}</td>
                </tr>
                </tbody>
            </table>
        </div>
        <div class="panel-footer">
            <hr>
            <a href="#" class="btn btn-success btn-large">générer le PDF | إنشاء بدف</a>
            <hr>
            <a href="{{ path('etudiantDeug_edit', { 'id': etudiantDeug.id }) }}" class="btn btn-info pull-left">Modifier | تغيير</a>
            {{ form_start(delete_form) }}
            <input type="submit" class="btn btn-danger pull-right" value="Effacer | حذف">
            {{ form_end(delete_form) }}
            <hr>
            <hr>
        </div>

    </div>
{% endblock %}